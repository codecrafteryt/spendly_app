/// App-wide feature flags.
///
/// Set [firebaseEnabled] to `true` after running:
///   flutterfire configure
/// and adding `google-services.json` (Android) / `GoogleService-Info.plist` (iOS).
class AppConfig {
  AppConfig._();

  static const firebaseEnabled = false;
}
